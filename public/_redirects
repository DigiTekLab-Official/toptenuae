# Cloudflare Pages static/external redirects only.
# SSR content migrations are handled by src/middleware.ts so normalization and
# exact legacy routing happen in one response.

/studio https://universal-studio.sanity.studio 301
